command: ["streamlit", "run", "app.py", "--server.port", "8000", "--server.address", "0.0.0.0"]

env:
  - name: DATABRICKS_WAREHOUSE_ID
    valueFrom:
      secretRef:
        scope: social-sources-platform
        key: DATABRICKS_WAREHOUSE_ID
